# Local runtime / generated state
.uv-cache/
**/.uv-cache/
**/__pycache__/
**/.venv/
**/.pytest_cache/
**/.mypy_cache/
**/*.pyc
.nexus/
.claude/

# Per-user workspace and nested repo content
nexus-workspace/

# Keep the user workspaces isolated, but track the VeriSight dependency as a submodule
# nexus-resources/ip/VeriSight/ is intentionally not ignored here
